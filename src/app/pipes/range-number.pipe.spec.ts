import { RangeNumberPipe } from './range-number.pipe';

describe('RangeNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new RangeNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
